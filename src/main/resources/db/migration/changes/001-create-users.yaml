databaseChangeLog:
  - changeSet:
      id: 1
      author: Sabitov
      changes:
        - createTable:
            tableName: users
            columns:
              - column:
                  name: id
                  type: uuid
                  constraints:
                    primaryKey: true
              - column:
                  name: name
                  type: varchar(50)
              - column:
                  name: password
                  type: varchar(255)
              - column:
                  name: email
                  type: varchar(50)
                  constraints:
                    unique: true
              - column:
                  name: role
                  type: varchar(50)